# Configuración local con valores reales (nunca versionar)
config.env
init/config.env
agents/*/config.env
optional/*/config.env

# Secretos de servidores MCP
.mcp.env
mcp/.mcp.env

# Archivos sensibles
.env
*.pem
*.key
credentials.json
secrets.*
